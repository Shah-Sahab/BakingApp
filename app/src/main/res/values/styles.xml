<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarStyle">@style/Toolbar</item>

    </style>

    <style name="Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="logo">@drawable/ic_bakery</item>
        <item name="titleTextAppearance">@style/AppTheme.Toolbar.Title</item>
        <!--<item name="titleMarginStart">15dp</item>-->
        <item name="displayOptions">useLogo|showHome</item>
    </style>

    <style name="AppTheme.Toolbar.Title" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/colorLogo</item>
        <item name="android:textStyle">italic</item>
        <!--<item name="app:titleMarginStart">5dp</item>-->
    </style>

    <style name="recycler_parent_style">
        <!--<item name="android:background">@drawable/item_touch_selector</item>-->
        <item name="android:background">?attr/selectableItemBackground</item>
        <!--<item name="android:background">@color/colorPrimaryDark</item>-->
    </style>

    <style name="recycler_item_style" parent="recycler_parent_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?android:attr/listPreferredItemHeight</item>
        <item name="android:layout_margin">@dimen/recipe_item_margin</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
